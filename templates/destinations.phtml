<?php require_once 'templates/header.phtml'; ?>

<div class="container mt-4">
    <h2 class="text-center">Destinos Populares</h2>
    
    <div class="row">
        <?php foreach($destinations as $destination): ?>
        <div class="col-md-4 mb-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title"><?php echo $destination->name; ?></h5>
                    <p class="card-text"><?php echo $destination->description; ?></p>
                    <p><strong>Atracciones:</strong> <?php echo $destination->attractions; ?></p>
                    <p><strong>Temporada alta:</strong> <?php echo ucfirst($destination->season); ?></p>
                </div>
                <div class="card-footer">
                <?php if (isset($_SESSION['IDUSUARIO']) && $_SESSION['IDUSUARIO'] == true): ?>
                    <a href="aditDestination/<?php echo $destination->IDDESTINATION; ?>" class="btn btn-warning" type="submit">Editar</a>
                    <a href="deleteDestination/<?php echo $destination->IDDESTINATION; ?>" class="btn btn-danger">Eliminar</a>
                    <?php else: ?>
                        <p class="text-muted">Inicia sesión para editar o eliminar un destino.</p>
                    <?php endif; ?>
                </div>
            </div>
        </div>
        <?php endforeach; ?>
    </div>
</div>


